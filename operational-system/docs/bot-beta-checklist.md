# בטא מבוקר — בוט מכירות

## לפני הפעלה

1. הרץ migrations: `018_funnel_events.sql`, `019_pending_followups.sql`
2. ודאי ב-`.env.local`: `OPENROUTER_API_KEY`, `MANYCHAT_WEBHOOK_SECRET`, `CALCOM_BOOKING_URL`
3. אופציונלי: `SLACK_WEBHOOK_URL`, `CRON_SECRET` (ל-cron silent leads)

## 10–20 לידים לבדיקה

לכל שיחה, סמני:

| מדד | עבר / נכשל |
|-----|------------|
| ליד חם שביקש פגישה → קיבל לינק תוך 2 הודעות | |
| אין "שמחה שפנית" / "לא ראיתי תשובה" | |
| אין הזיית כאב | |
| מחיר: גישת אפיון 350 ₪ + בוט 500–4500 ₪ | |
| ליד לא רלוונטי → סגירה אדיבה | |

## קריטריון עלייה ל-production

- hot → פגישה > 70%
- הזיות כאב = 0
- ציון טבעיות > 8/10 (סקירה ידנית)
